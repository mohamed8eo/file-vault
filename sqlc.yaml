version: "2"

sql:
  - engine: "postgresql"
    queries: "migrations/queries/"
    schema: "migrations/schema/"
    gen:
      go:
        package: "db"
        out: "internal/db"
        sql_package: "pgx/v5"
